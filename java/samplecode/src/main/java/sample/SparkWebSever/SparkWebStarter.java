package sample.SparkWebSever;

//
//import static spark.Spark.*;

public class SparkWebStarter {
    public static void main(String[] args) {

        // 포트 설정
   /*     port(9999);

        // 외부 영역의 경로를 설정하여 static 파일을 접근할 수 있께 함
        staticFiles.externalLocation("/Users/dream/Test/webstatic");

        int maxThread = 10;
        int minThread = 2;
        int timeOutMillis = 30000;

        threadPool(maxThread, minThread, timeOutMillis);

        get("/hello", (req, res) -> "Hello World");*/

    }
}
