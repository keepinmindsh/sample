server:
  port: 9000

spring:
  datasource:
    url: jdbc:h2:file:/Users/dream/GIT/sample/templates/lines-card-handler/test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false

